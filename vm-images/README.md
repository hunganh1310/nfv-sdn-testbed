# VM Images

This folder holds **cloud images** and **VM disk files** used by the lab.

> ⚠️ **Contents are git-ignored** — do not commit `.qcow2`, `.img`, `.iso` files.

Expected contents (downloaded during relevant phases):
- `ubuntu-22.04-server-cloudimg-amd64.img`
- `node-0.qcow2`, `node-1.qcow2`, `node-2.qcow2`
